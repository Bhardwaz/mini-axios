# mini-axios

just trying to build a mini version of axios from scratch to understand how it actually works under the hood. 

doing this for about 30 mins a day. currently looking into how they handle JS type checking quirks (since standard `typeof` is pretty bad) before making the actual network requests.

**todo:**
- [x] solid type checkers
- [ ] format request/response data automatically
- [ ] wrapper for XHR using promises
- [ ] core methods (.get, .post)
- [ ] interceptors (promise chaining)

vanilla js only. no dependencies.